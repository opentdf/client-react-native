const path = require('path');

module.exports = {
  dependencies: {
    'react-native-opentdf': {
      root: path.join(__dirname, '..'),
    },
    'react-native-vector-icons': {
      platforms: {
        ios: null,
      },
    },
  },
  
};
